import{_ as t,o,c as d,a3 as r}from"./chunks/framework.BzWmN0d8.js";const h=JSON.parse('{"title":"迁移到 lograil","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/migrating.md","filePath":"zh/guide/migrating.md","lastUpdated":1788425513000}'),a={name:"zh/guide/migrating.md"};function c(s,e,i,n,l,p){return o(),d("div",null,[...e[0]||(e[0]=[r(`<h1 id="迁移到-lograil" tabindex="-1">迁移到 lograil <a class="header-anchor" href="#迁移到-lograil" aria-label="Permalink to &quot;迁移到 lograil&quot;">​</a></h1><p>本页将 <code>electron-log</code>、<code>winston</code>、<code>pino</code> 的常见用法映射到 <code>lograil</code>，并指出行为差异，避免踩坑。</p><h2 id="从-electron-log-迁移" tabindex="-1">从 <code>electron-log</code> 迁移 <a class="header-anchor" href="#从-electron-log-迁移" aria-label="Permalink to &quot;从 \`electron-log\` 迁移&quot;">​</a></h2><p>和 <code>electron-log</code> 一样，<code>lograil</code> 在 Electron 下是<strong>零配置</strong>的——单个 <code>createLogger()</code> （或默认的 <code>logger</code>）会自动探测自己运行在主进程还是渲染进程，并替你接好一切。 <strong>不需要</strong>配置 <code>transports</code>，也<strong>不需要</strong>写 IPC 对接代码：</p><ul><li>在<strong>渲染进程</strong>中，日志既写本地 console，又自动经 IPC 转发到主进程。</li><li>在<strong>主进程</strong>中，日志写 console 和 应用 <code>logs</code> 目录（<code>app.getPath(&#39;logs&#39;)</code>）下的按日轮转文件；渲染进程 日志经 IPC 接收后写入<strong>独立</strong>的 <code>renderer.{date}.log</code>，两个进程永不混在一起。</li></ul><table tabindex="0"><thead><tr><th><code>electron-log</code></th><th><code>lograil</code>（零配置默认）</th></tr></thead><tbody><tr><td><code>log.info(&#39;hi&#39;, meta)</code></td><td><code>logger.info(&#39;hi&#39;, meta)</code></td></tr><tr><td><code>log.scope(&#39;worker&#39;)</code></td><td><code>logger.scope(&#39;worker&#39;, { /* ctx */ })</code> / <code>logger.child({ context })</code></td></tr><tr><td><code>log.transports.console.level = &#39;info&#39;</code></td><td>在 logger 上设 <code>level</code>——console transport 已自动添加</td></tr><tr><td><code>log.transports.file.fileName = &#39;app.log&#39;</code></td><td>自动：<code>&lt;logs&gt;/main.log</code>（+ <code>renderer.log</code>）</td></tr><tr><td><code>log.transports.console.format = &#39;[{level}] {text}&#39;</code></td><td><code>ConsoleTransport</code> 的 formatter（通过自定义 runtime 覆盖）</td></tr><tr><td><code>log.variables = { ... }</code> / <code>log.context</code></td><td><code>logger.setContext(key, value)</code> / <code>mergeContext</code></td></tr><tr><td><code>log.hooks.process</code>（改写条目）</td><td>带 <code>onEntry</code> 的 <code>Plugin</code></td></tr><tr><td><code>log.transports.remote</code>（渲染→主进程）</td><td><strong>自动</strong> IPC 转发——无需写代码</td></tr></tbody></table><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.ts 与 renderer.ts 相同，零配置：</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;lograil&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;在两个进程都能直接用&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><strong>差异</strong></p><ul><li><code>electron-log</code> 把 IPC 这一跳藏在一组固定 transport 后面；<code>lograil</code> 通过自身的 Electron runtime 执行同样的跳转，且只在你选择自定义 runtime 时才把它<strong>显式化</strong>。两者都不复制数据， 参见不可变性)。</li><li>级别固定为 <code>trace…fatal</code>，不像 electron-log 那样是可自定义的字符串映射。用 <code>setLevel</code> 映射旧名称。</li><li><code>lograil</code> 区分 <code>context</code>（持久）与 <code>metadata</code>（一次性），并在条目抵达 transport 前<strong>冻结</strong>， 因此插件/transport 无法改共享状态。</li></ul><h3 id="进阶-手动控制-可选" tabindex="-1">进阶：手动控制（可选） <a class="header-anchor" href="#进阶-手动控制-可选" aria-label="Permalink to &quot;进阶：手动控制（可选）&quot;">​</a></h3><p>可以关闭或替换自动接线。仅当你选择退出（<code>forwardToMain: false</code> / <code>receiveFromRenderer: false</code>） 或用非 Electron 的 runtime 构造 logger 时才需要：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 渲染进程——手动转发（替代默认渲染 runtime）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ElectronIpcTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 主进程——手动接收（替代默认主 runtime）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { registerIpcReceiver } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;lograil&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerIpcReceiver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">entry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ingestEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entry));</span></span></code></pre></div><p>完整零配置流程与安全 preload 配置见 <a href="./../guide/runtime-electron">Electron 指南</a>。</p><h2 id="从-winston-迁移" tabindex="-1">从 <code>winston</code> 迁移 <a class="header-anchor" href="#从-winston-迁移" aria-label="Permalink to &quot;从 \`winston\` 迁移&quot;">​</a></h2><p><code>winston</code> 通过 <code>format.combine(...)</code> 与每个实例的 transports 组合；<code>lograil</code> 使用单一管道（<code>Filter</code>/<code>Processor</code>/<code>Formatter</code>）+ transports。</p><table tabindex="0"><thead><tr><th><code>winston</code></th><th><code>lograil</code></th></tr></thead><tbody><tr><td><code>createLogger({ transports: [new transports.Console(), new transports.File()] })</code></td><td><code>createLogger({ transports: [new ConsoleTransport(), new FileTransport({ mode: &#39;rotate-time&#39;, appName, unit: &#39;day&#39; })] })</code></td></tr><tr><td><code>logger.log(&#39;info&#39;, msg, meta)</code></td><td><code>logger.info(msg, meta)</code></td></tr><tr><td><code>format.combine(format.json(), …)</code></td><td><code>createJsonFormatter()</code>（或 <code>createJsonFormatter({ flatten: true })</code>）</td></tr><tr><td><code>format.timestamp()</code> / <code>format.colorize()</code></td><td>内置于 <code>createLineFormatter()</code></td></tr><tr><td><code>logger.child({ module })</code></td><td><code>logger.scope(...)</code> / <code>logger.child({ context })</code></td></tr><tr><td><code>transports.Http</code></td><td><code>OtlpTransport</code> 或自定义 <code>fetch</code> transport</td></tr><tr><td>自定义 <code>Transport</code> 子类</td><td>实现 <code>Transport</code> 接口（<code>write(entry, formatted)</code>）</td></tr></tbody></table><p><strong>差异</strong></p><ul><li><code>winston</code> 在每个 transport 上各自 <code>format</code>；<code>lograil</code> 在管道里<strong>统一格式化一次</strong>，transport 也可用自身 <code>formatter</code> 覆盖。</li><li><code>winston</code> 的 <code>meta</code> 对应 <code>lograil</code> 的 <code>context</code>/<code>metadata</code>。消息后的 <code>args</code> 是位置参数（<code>logger.info(&#39;msg&#39;, a, b)</code> → <code>entry.args = [a, b]</code>）。</li><li><code>lograil</code> 的 processor 可在格式化前归一化值（<code>createDefaultSerializers</code>）并脱敏（<code>createRedactProcessor</code>）。</li></ul><h2 id="从-pino-迁移" tabindex="-1">从 <code>pino</code> 迁移 <a class="header-anchor" href="#从-pino-迁移" aria-label="Permalink to &quot;从 \`pino\` 迁移&quot;">​</a></h2><p><code>pino</code> 是仅限 Node、极快的裸日志库。<code>lograil</code> 用少量纯 Node 吞吐换取<strong>一流的 Web/Node/Electron 全平台支持</strong>、结构化 <code>context</code>/<code>metadata</code>、插件管道与跨进程 IPC。</p><table tabindex="0"><thead><tr><th><code>pino</code></th><th><code>lograil</code></th></tr></thead><tbody><tr><td><code>pino({ level })</code></td><td><code>createLogger({ level })</code></td></tr><tr><td><code>pino({ transport: { target: &#39;pino/file&#39;, options: { destination } } })</code></td><td><code>new FileTransport({ mode: &#39;rotate-time&#39;, appName, unit: &#39;day&#39;, dir })</code></td></tr><tr><td><code>pino-pretty</code></td><td><code>ConsoleTransport</code> + <code>createLineFormatter()</code></td></tr><tr><td><code>logger.child({ reqId })</code></td><td><code>logger.child({ context: { reqId } })</code></td></tr><tr><td><code>logger.info({ foo }, &#39;msg&#39;)</code></td><td><code>logger.info(&#39;msg&#39;, { foo })</code>（或 <code>logger.child({ context: { foo } })</code>）</td></tr><tr><td><code>pino.destination</code> / <code>sonic-boom</code></td><td><code>FileTransport</code>（Node <code>fs</code>）</td></tr><tr><td><code>pino</code> transports（worker 线程）</td><td><code>ElectronIpcTransport</code> / <code>OtlpTransport</code></td></tr></tbody></table><p><strong>差异</strong></p><ul><li><code>pino</code> 仅运行于 Node；<code>lograil</code> 可运行在 Electron 主进程、Electron 渲染进程（经 <code>ElectronIpcTransport</code> 到主进程）以及 Web。</li><li><code>pino</code> 在纯 Node 吞吐上更快（C 层缓冲）。若你只在服务端日志且不涉及 Electron/浏览器，<code>pino</code> 也是好选择。</li><li><code>lograil</code> 额外提供：processor/插件管道、内置脱敏与序列化器、OTel trace 关联（<code>createOtelTracePlugin</code>）、按命名空间（模块名）过滤，以及冻结的不可变条目契约。</li></ul><h2 id="通用映射" tabindex="-1">通用映射 <a class="header-anchor" href="#通用映射" aria-label="Permalink to &quot;通用映射&quot;">​</a></h2><table tabindex="0"><thead><tr><th>概念</th><th><code>lograil</code> API</th></tr></thead><tbody><tr><td>输出</td><td><code>logger.trace/debug/info/warn/error/fatal</code></td></tr><tr><td>级别控制</td><td><code>setLevel</code>、transport 级 <code>level</code></td></tr><tr><td>结构化字段</td><td><code>context</code>（持久）/ <code>metadata</code>（一次性）/ <code>args</code></td></tr><tr><td>子 logger</td><td><code>scope(name, ctx)</code> / <code>child({ context, level })</code></td></tr><tr><td>钩子 / 中间件</td><td><code>Plugin</code>（<code>onEntry</code>、<code>onInit</code>、<code>onTransport</code>、<code>onDestroy</code>）</td></tr><tr><td>采样</td><td><code>createSampler</code>（filter）</td></tr><tr><td>脱敏</td><td><code>createRedactProcessor</code></td></tr><tr><td>自定义落盘</td><td>实现 <code>Transport</code></td></tr></tbody></table><p>完整 API 见<a href="./../guide/transports">传输器</a>、<a href="./../api/pipeline">管道</a>与<a href="./../guide/plugins">插件</a>指南。</p>`,26)])])}const k=t(a,[["render",c]]);export{h as __pageData,k as default};
