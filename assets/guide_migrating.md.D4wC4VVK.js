import{_ as o,o as t,c as r,a3 as d}from"./chunks/framework.BzWmN0d8.js";const h=JSON.parse('{"title":"Migrating to lograil","description":"","frontmatter":{},"headers":[],"relativePath":"guide/migrating.md","filePath":"guide/migrating.md","lastUpdated":1788184774000}'),a={name:"guide/migrating.md"};function i(s,e,n,c,l,p){return t(),r("div",null,[...e[0]||(e[0]=[d(`<h1 id="migrating-to-lograil" tabindex="-1">Migrating to lograil <a class="header-anchor" href="#migrating-to-lograil" aria-label="Permalink to &quot;Migrating to lograil&quot;">​</a></h1><p>This page maps common patterns from <code>electron-log</code>, <code>winston</code> and <code>pino</code> to <code>lograil</code>, and calls out behavioral differences so you don&#39;t get surprised.</p><h2 id="from-electron-log" tabindex="-1">From <code>electron-log</code> <a class="header-anchor" href="#from-electron-log" aria-label="Permalink to &quot;From \`electron-log\`&quot;">​</a></h2><p>Like <code>electron-log</code>, <code>lograil</code> is <strong>zero-config in Electron</strong> — a single <code>createLogger()</code> (or the default <code>logger</code>) auto-detects whether it runs in the main or renderer process and wires everything for you. There is <strong>no</strong> <code>transports</code> to configure and <strong>no</strong> IPC wiring to write:</p><ul><li>In the <strong>renderer</strong>, logs go to the local console <em>and</em> are forwarded to the main process over IPC automatically.</li><li>In the <strong>main</strong>, logs go to the console <em>and</em> a daily rotating file under the app&#39;s <code>logs</code> directory (<code>app.getPath(&#39;logs&#39;)</code>); renderer entries are received over IPC and written to a <strong>separate</strong> <code>renderer.{date}.log</code> so the two processes never mix.</li></ul><table tabindex="0"><thead><tr><th><code>electron-log</code></th><th><code>lograil</code> (zero-config default)</th></tr></thead><tbody><tr><td><code>log.info(&#39;hi&#39;, meta)</code></td><td><code>logger.info(&#39;hi&#39;, meta)</code></td></tr><tr><td><code>log.scope(&#39;worker&#39;)</code></td><td><code>logger.scope(&#39;worker&#39;, { /* ctx */ })</code> / <code>logger.child({ context })</code></td></tr><tr><td><code>log.transports.console.level = &#39;info&#39;</code></td><td>set <code>level</code> on the logger — the console transport is added automatically</td></tr><tr><td><code>log.transports.file.fileName = &#39;app.log&#39;</code></td><td>automatic: <code>&lt;logs&gt;/main.log</code> (+ <code>renderer.log</code>)</td></tr><tr><td><code>log.transports.console.format = &#39;[{level}] {text}&#39;</code></td><td><code>ConsoleTransport</code> formatter (override via a custom runtime)</td></tr><tr><td><code>log.variables = { ... }</code> / <code>log.context</code></td><td><code>logger.setContext(key, value)</code> / <code>mergeContext</code></td></tr><tr><td><code>log.hooks.process</code> (rewrite entries)</td><td>a <code>Plugin</code> with <code>onEntry</code></td></tr><tr><td><code>log.transports.remote</code> (renderer→main)</td><td><strong>automatic</strong> IPC forwarding — no code needed</td></tr></tbody></table><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main.ts and renderer.ts — identical, zero-config:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;lograil&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;just works in both processes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><strong>Differences</strong></p><ul><li><code>electron-log</code> hides the IPC hop behind fixed transports; <code>lograil</code> performs the same hop automatically via its Electron runtime, and only <em>surfaces</em> it when you opt into a custom runtime (see below). Both avoid copying (zero-copy) — see <a href="./../guide/immutability">Immutability &amp; zero-copy</a>.</li><li>Levels are fixed (<code>trace…fatal</code>), not a configurable string map like electron-log. Map old names with <code>setLevel</code>.</li><li><code>lograil</code> separates <code>context</code> (persistent) from <code>metadata</code> (one-off) and <strong>freezes</strong> entries before they reach transports, so plugins/transports can&#39;t mutate shared state.</li></ul><h3 id="advanced-manual-control-optional" tabindex="-1">Advanced: manual control (optional) <a class="header-anchor" href="#advanced-manual-control-optional" aria-label="Permalink to &quot;Advanced: manual control (optional)&quot;">​</a></h3><p>The automatic wiring can be turned off or replaced. Reach for this only when you opt out (<code>forwardToMain: false</code> / <code>receiveFromRenderer: false</code>) or build the logger with a non-Electron runtime:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// renderer — forward manually (instead of the default renderer runtime)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ElectronIpcTransport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// main — receive manually (instead of the default main runtime)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { registerIpcReceiver } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;lograil&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerIpcReceiver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">entry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ingestEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(entry));</span></span></code></pre></div><p>See the <a href="./../guide/runtime-electron">Electron guide</a> for the full zero-config flow and the secure preload setup.</p><h2 id="from-winston" tabindex="-1">From <code>winston</code> <a class="header-anchor" href="#from-winston" aria-label="Permalink to &quot;From \`winston\`&quot;">​</a></h2><p><code>winston</code> composes <code>format.combine(...)</code> and transports per instance. <code>lograil</code> uses a single pipeline (<code>Filter</code>/<code>Processor</code>/<code>Formatter</code>) plus transports.</p><table tabindex="0"><thead><tr><th><code>winston</code></th><th><code>lograil</code></th></tr></thead><tbody><tr><td><code>createLogger({ transports: [new transports.Console(), new transports.File()] })</code></td><td><code>createLogger({ transports: [new ConsoleTransport(), new FileTransport({ mode: &#39;rotate-time&#39;, appName, unit: &#39;day&#39; })] })</code></td></tr><tr><td><code>logger.log(&#39;info&#39;, msg, meta)</code></td><td><code>logger.info(msg, meta)</code></td></tr><tr><td><code>format.combine(format.json(), …)</code></td><td><code>createJsonFormatter()</code> (or <code>createJsonFormatter({ flatten: true })</code>)</td></tr><tr><td><code>format.timestamp()</code> / <code>format.colorize()</code></td><td>built into <code>createLineFormatter()</code></td></tr><tr><td><code>logger.child({ module })</code></td><td><code>logger.scope(...)</code> / <code>logger.child({ context })</code></td></tr><tr><td><code>transports.Http</code></td><td><code>OtlpTransport</code> or a custom <code>fetch</code> transport</td></tr><tr><td>custom <code>Transport</code> subclass</td><td>implement the <code>Transport</code> interface (<code>write(entry, formatted)</code>)</td></tr></tbody></table><p><strong>Differences</strong></p><ul><li><code>winston</code> formats per-transport via each transport&#39;s <code>format</code>. <code>lograil</code> formats <strong>once</strong> in the pipeline; a transport may override with its own <code>formatter</code>.</li><li><code>winston</code> <code>meta</code> becomes <code>lograil</code> <code>context</code>/<code>metadata</code>. <code>args</code> after the message are positional (<code>logger.info(&#39;msg&#39;, a, b)</code> → <code>entry.args = [a, b]</code>).</li><li><code>lograil</code>&#39;s processors can normalize values (<code>createDefaultSerializers</code>) and redact secrets (<code>createRedactProcessor</code>) before formatting.</li></ul><h2 id="from-pino" tabindex="-1">From <code>pino</code> <a class="header-anchor" href="#from-pino" aria-label="Permalink to &quot;From \`pino\`&quot;">​</a></h2><p><code>pino</code> is a Node-only, extremely fast raw logger. <code>lograil</code> trades a little raw Node throughput for first-class <strong>Web/Node/Electron</strong> support, structured context/metadata, a plugin pipeline, and zero-copy IPC.</p><table tabindex="0"><thead><tr><th><code>pino</code></th><th><code>lograil</code></th></tr></thead><tbody><tr><td><code>pino({ level })</code></td><td><code>createLogger({ level })</code></td></tr><tr><td><code>pino({ transport: { target: &#39;pino/file&#39;, options: { destination } } })</code></td><td><code>new FileTransport({ mode: &#39;rotate-time&#39;, appName, unit: &#39;day&#39;, dir })</code></td></tr><tr><td><code>pino-pretty</code></td><td><code>ConsoleTransport</code> + <code>createLineFormatter()</code></td></tr><tr><td><code>logger.child({ reqId })</code></td><td><code>logger.child({ context: { reqId } })</code></td></tr><tr><td><code>logger.info({ foo }, &#39;msg&#39;)</code></td><td><code>logger.info(&#39;msg&#39;, { foo })</code> (or <code>logger.child({ context: { foo } })</code>)</td></tr><tr><td><code>pino.destination</code> / <code>sonic-boom</code></td><td><code>FileTransport</code> (Node <code>fs</code>)</td></tr><tr><td><code>pino</code> transports (worker thread)</td><td><code>ElectronIpcTransport</code> / <code>OtlpTransport</code></td></tr></tbody></table><p><strong>Differences</strong></p><ul><li><code>pino</code> runs only on Node. <code>lograil</code> runs on Electron main, Electron renderer (via <code>ElectronIpcTransport</code> to the main process), and the Web.</li><li><code>pino</code> is faster for pure Node throughput (C-level buffering). If you only log server-side and never touch Electron/browsers, <code>pino</code> is a fine choice.</li><li><code>lograil</code> adds: a processor/plugin pipeline, built-in redaction + serializers, OTel trace correlation (<code>createOtelTracePlugin</code>), namespace (module) filtering, and a frozen immutable entry contract.</li></ul><h2 id="general-mapping" tabindex="-1">General mapping <a class="header-anchor" href="#general-mapping" aria-label="Permalink to &quot;General mapping&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concept</th><th><code>lograil</code> API</th></tr></thead><tbody><tr><td>emit</td><td><code>logger.trace/debug/info/warn/error/fatal</code></td></tr><tr><td>level control</td><td><code>setLevel</code>, per-transport <code>level</code></td></tr><tr><td>structured fields</td><td><code>context</code> (persistent) / <code>metadata</code> (one-off) / <code>args</code></td></tr><tr><td>child logger</td><td><code>scope(name, ctx)</code> / <code>child({ context, level })</code></td></tr><tr><td>hooks / middleware</td><td><code>Plugin</code> (<code>onEntry</code>, <code>onInit</code>, <code>onTransport</code>, <code>onDestroy</code>)</td></tr><tr><td>sampling</td><td><code>createSampler</code> (filter)</td></tr><tr><td>redaction</td><td><code>createRedactProcessor</code></td></tr><tr><td>custom sink</td><td>implement <code>Transport</code></td></tr></tbody></table><p>See the <a href="./../guide/transports">Transports</a>, <a href="./../api/pipeline">Pipeline</a> and <a href="./../guide/plugins">Plugins</a> guides for the full surface.</p>`,26)])])}const m=o(a,[["render",i]]);export{h as __pageData,m as default};
